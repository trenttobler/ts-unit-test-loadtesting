import { expect } from 'chai';
import { one } from '../../../stub';

it('succeed one', () => {
  expect(one).to.equal("one");
});

it('succeed two', () => {
  expect(one).to.equal("one");
});

it('succeed three', () => {
  expect(one).to.equal("one");
});

it('succeed four', () => {
  expect(one).to.equal("one");
});

it('succeed five', () => {
  expect(one).to.equal("one");
});

it('succeed six', () => {
  expect(one).to.equal("one");
});

it('succeed seven', () => {
  expect(one).to.equal("one");
});

it('succeed eight', () => {
  expect(one).to.equal("one");
});
