import { expect } from 'chai';
import { one } from '../../../stub';

describe("section 1", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 2", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 3", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 4", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 5", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 6", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 7", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 8", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})

describe("section 9", () => {
	describe("part I", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part II", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part III", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})

	describe("part IV", () => {
		it('succeed one', () => {
			expect(one).to.equal("one");
		});

		it('succeed two', () => {
			expect(one).to.equal("one");
		});

		it('succeed three', () => {
			expect(one).to.equal("one");
		});

		it('succeed four', () => {
			expect(one).to.equal("one");
		});

		it('succeed five', () => {
			expect(one).to.equal("one");
		});

		it('succeed six', () => {
			expect(one).to.equal("one");
		});

		it('succeed seven', () => {
			expect(one).to.equal("one");
		});

		it('succeed eight', () => {
			expect(one).to.equal("one");
		});
	})
})