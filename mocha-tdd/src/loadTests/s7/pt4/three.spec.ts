import { expect } from 'chai';
import { one } from '../../../stub';

it('succeed one', () => {
  expect(one).to.equal("one");
});

it('succeed two', () => {
  expect(one).to.equal("one");
});

it('succeed three', () => {
  expect(one).to.equal("one");
});

it('succeed four', () => {
  expect(one).to.equal("one");
});
