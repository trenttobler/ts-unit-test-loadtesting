import { one } from '../../../stub';

test('succeed one', () => {
  expect(one).toEqual("one");
});

test('succeed two', () => {
  expect(one).toEqual("one");
});

test('succeed three', () => {
  expect(one).toEqual("one");
});

test('succeed four', () => {
  expect(one).toEqual("one");
});
